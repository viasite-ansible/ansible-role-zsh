---
- name: Install zsh, git
  homebrew:
    name: "{{ item }}"
    state: present
  with_items:
    - zsh
    - git

- name: Install tmux
  homebrew:
    name: tmux
    state: present
  when: "'tmux' in zsh_plugins"
